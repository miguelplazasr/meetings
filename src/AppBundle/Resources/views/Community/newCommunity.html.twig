{% extends '::base.html.twig' %}

{% block body %}
    {%  if (form is not null) %}
    {{ form_start(form, {'action': path('post_community'), 'method': 'POST'}) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" class="btn btn-default pull-right"/>
        {% endif %}
{% endblock %}